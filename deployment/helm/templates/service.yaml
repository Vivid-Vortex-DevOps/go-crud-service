apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-crud-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "go-crud-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "go-crud-service.selectorLabels" . | nindent 4 }}
